version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
      - "2809:2809"
    environment:
      - OMNIORB_CONFIG=/etc/omniorb.cfg
    volumes:
      - ./backend:/app
